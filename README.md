# 🧑‍💻 FastMCP 설치 가이드

### **FastMCP `install` 명령어 사용법 변경 (버전 2.10.3 기준)**

FastMCP 버전 **2.10.3**을 기준으로 서버 설치 명령어의 형식이 변경되었습니다.

-----

#### 1️⃣ 버전 2.10.3 이전: 클라이언트 이름 생략 가능

  * **명령어 형식:** `fastmcp install <파일 이름>`

  * **특징:** 버전 2.10.3 이전까지는 설치 대상이 **Claude Desktop**인 경우, 클라이언트 이름 명시 없이 파일 이름만으로 서버 설치가 가능했습니다.

    > **예시:**

    > ```bash
    > fastmcp install server.py
    > ```

-----

#### 2️⃣ 버전 2.10.3 이후: 클라이언트 이름 명시 필수

  * **명령어 형식:** `fastmcp install <클라이언트 이름> <파일 이름>`

  * **특징:** 버전 2.10.3부터 **여러 MCP 클라이언트**가 지원됨에 따라, 설치 대상이 **어떤 클라이언트**용 서버인지 명확하게 지정해야 합니다. 따라서, 설치 명령어에 클라이언트 이름을 포함하는 것이 필수가 되었습니다.

  * **주의:** 이제 `fastmcp install` 명령어에 클라이언트 이름을 포함하지 않으면 **오류가 발생**합니다.

    > **예시 (Claude Desktop용 서버 설치 시):**

    > ```bash
    > fastmcp install claude-desktop server.py
    > ```

-----

### **FastMCP 환경 설정 및 설치 워크플로우**

FastMCP 서버를 설치하고 실행하기 위한 기본적인 워크플로우입니다.

#### 1\. 🐍 파이썬 설치 및 확인

  * **필수:** 파이썬이 시스템에 우선적으로 설치되어 있어야 합니다.
  * **확인:** 터미널 또는 명령 프롬프트에서 아래 명령어를 입력하여 설치 및 버전을 확인하세요.
    ```bash
    python --version
    ```

#### 2\. 📦 필요한 라이브러리 설치 (로컬 터미널에서 이용)

  * `requirements.txt` 파일이 있는 폴더에 접근하여 필요한 라이브러리를 설치합니다.
    ```bash
    python -m pip install -r requirements.txt
    ```

#### 3\. 🛡️ 가상 환경(Virtual Environment) 세팅

  * **가상 환경 생성:** 프로젝트 폴더 내에 `venv`라는 이름의 가상 환경을 생성합니다.
    ```bash
    python -m venv venv
    ```
  * **가상 환경 활성화:** 생성된 가상 환경을 활성화합니다.
    ```bash
    .\venv\Scripts\activate  # Windows (명령 프롬프트/PowerShell)
    # source venv/bin/activate  # macOS/Linux (Bash/Zsh)
    ```
  * **FastMCP 패키지 설치:** 활성화된 가상 환경 내에 `fastmcp`를 설치합니다.
    ```bash
    pip install fastmcp
    ```

#### 4\. ⚙️ MCP 서버 설치

  * 각 MCP 서버 파일을 설치할 때, 아래와 같은 명령어로 설치를 완료합니다.
    ```bash
    fastmcp install claude-desktop <MCP서버 파일 이름>
    ```
